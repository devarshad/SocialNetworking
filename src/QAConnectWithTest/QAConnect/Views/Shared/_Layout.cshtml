<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
</head>
<body>
    @Html.Action("AlertMessages", "Base")
    @RenderBody()
    @Scripts.Render("~/bundles/jBase")
    @Styles.Render("~/Content/cBase")
    @RenderSection("Account", required: false)
    @RenderSection("UserIn", required: false)
</body>
</html>